<script>
(() => {
const config={multiple:@json($multiple),treeUrl:@json(url('load-tree/' . $workstationApp . '/' . $leaf)),initialScope:@json($loadWsId ? 'ws-' . $loadWsId : null),csrf:@json(csrf_token()),initialTreeData:@json($treeData)};
const fieldIds=['units','LumUnits','AmbientLight','AmbientStable','PutDisplaysToEnergySaveMode','StartEnergySaveMode','EndEnergySaveMode','CalibrationPresents','CalibrationType','ColorTemperatureAdjustment','ColorTemperatureAdjustment_ext','WhiteLevel_u_extcombo','WhiteLevel_u_input','WhiteLevel','SetWhiteLevel','CreateICCICMProfile','UsedRegulation','UsedClassification','UsedClassificationForLastScheduling','UsedRegulationForLastScheduling','bodyRegion','AutoDailyTests','Facility','Department','Room','ResponsiblePersonName','ResponsiblePersonCity','ResponsiblePersonAddress','ResponsiblePersonEmail','ResponsiblePersonPhoneNumber'];
const state={facilities:[],workgroups:[],workstations:[],checkedScopes:new Set(),activeScope:config.initialScope&&String(config.initialScope).startsWith('ws-')?config.initialScope:null,search:'',facilityFilter:'',workgroupFilter:'',currentData:{},currentMeta:{workstationCount:0,singleWorkstation:false,scopeType:null,mixedFields:[]}};
const byId=(id)=>document.getElementById(id);
const setText=(id,value)=>{const el=byId(id);if(el)el.textContent=value;};
const introCard=()=>byId('settings-intro-card');
const workstationList=()=>byId('settings-workstation-list');
const bulkPanel=()=>byId('settings-bulk-panel');
const bulkList=()=>byId('settings-bulk-list');
const bulkEmpty=()=>byId('settings-bulk-empty');
const bulkCount=()=>byId('settings-bulk-count');
const normalizeSearch=(value)=>String(value||'').trim().toLowerCase();
const selectedBulkScope=()=>{if(state.checkedScopes.size===0)return null;const ids=Array.from(state.checkedScopes).map((scope)=>String(scope).replace(/^ws-/,'')).filter(Boolean);return ids.length?`list-${ids.join(',')}`:null;};
const workstationByScope=(scope)=>state.workstations.find((item)=>item.scope===scope);
const buildHierarchyData=(flatNodes)=>{const nodeMap=new Map(flatNodes.map((node)=>[node.id,node]));state.facilities=flatNodes.filter((node)=>node.type==='facility').map((node)=>({id:node.id,text:node.text})).sort((a,b)=>a.text.localeCompare(b.text));state.workgroups=flatNodes.filter((node)=>node.type==='workgroup').map((node)=>{const facility=nodeMap.get(node.parent);return{id:node.id,text:node.text,facilityId:facility?.id||'',facilityText:facility?.text||''};}).sort((a,b)=>a.text.localeCompare(b.text));state.workstations=flatNodes.filter((node)=>node.type==='workstation').map((node)=>{const workgroup=nodeMap.get(node.parent);const facility=workgroup?nodeMap.get(workgroup.parent):null;return{scope:node.id,id:String(node.id).replace(/^ws-/,''),text:String(node.text||'').trim(),workgroupId:workgroup?.id||'',workgroupText:workgroup?.text||'Unknown workgroup',facilityId:facility?.id||'',facilityText:facility?.text||'Unknown facility'};}).sort((a,b)=>{const byFacility=a.facilityText.localeCompare(b.facilityText);if(byFacility!==0)return byFacility;const byWorkgroup=a.workgroupText.localeCompare(b.workgroupText);if(byWorkgroup!==0)return byWorkgroup;return a.text.localeCompare(b.text);});};
const normalizeOptions=(raw)=>{if(!raw)return[];let parsed=raw;if(typeof raw==='string'){try{parsed=JSON.parse(raw);}catch(_){return[];}}if(Array.isArray(parsed)){return parsed.map((item)=>typeof item==='object'?{value:item.key??item.value??'',label:item.value??item.label??item.key??''}:{value:item,label:item});}return Object.entries(parsed).map(([value,label])=>({value,label}));};
const populateSelect=(id,options,selectedValue='',mixed=false)=>{const select=byId(id);if(!select)return;const normalized=normalizeOptions(options);select.innerHTML=mixed?'<option value="">Mixed values</option>':'<option value=""></option>';normalized.forEach((option)=>{const item=document.createElement('option');item.value=option.value;item.textContent=option.label;if(!mixed&&String(option.value)===String(selectedValue))item.selected=true;select.appendChild(item);});};
const applyMixedState=(id,isMixed)=>{const element=byId(id);if(!element)return;if(element.type==='checkbox'){element.classList.toggle('ring-2',isMixed);element.classList.toggle('ring-amber-300',isMixed);element.title=isMixed?'Mixed values':'';if(isMixed)element.checked=false;return;}if(element.tagName==='SELECT'){element.dataset.mixed=isMixed?'1':'0';element.classList.toggle('border-amber-200',isMixed);element.classList.toggle('bg-amber-50/60',isMixed);if(isMixed)populateSelect(id,element._optionsSource||[],'',true);return;}if(isMixed){element.value='';element.placeholder='Mixed values';element.classList.add('border-amber-200','bg-amber-50/60');}else{element.classList.remove('border-amber-200','bg-amber-50/60');if(element.placeholder==='Mixed values')element.placeholder='';}};
const toggleVisibility=(id,visible)=>{const element=byId(id);if(element)element.style.display=visible?'':'none';};
const setInputValue=(id,value)=>{const element=byId(id);if(!element)return;if(element.type==='checkbox'){element.checked=value===true||value===1||value==='1'||value==='true';}else{element.value=value??'';}};
const syncConditionalFields=()=>{toggleVisibility('energy-save-fields',byId('PutDisplaysToEnergySaveMode')?.checked);toggleVisibility('color-temperature-custom-wrap',byId('ColorTemperatureAdjustment')?.value==='20');toggleVisibility('white-level-custom-wrap',byId('WhiteLevel_u_extcombo')?.value==='custom');const regulation=byId('UsedRegulation')?.value;toggleVisibility('body-region-wrap',regulation==='DIN 6868-157');setText('labelRoomClass',regulation==='DIN 6868-157'?'Room Class':'Display Category');};
const setScopeMeta=(meta)=>{state.currentMeta=meta||{workstationCount:0,singleWorkstation:false,scopeType:null,mixedFields:[]};};
const updateSelectionSummary=()=>{if(state.checkedScopes.size>0){setText('settings-selection-label',`${state.checkedScopes.size} workstation selected`);setText('settings-selection-help','Bulk selection is active. Save to apply the same settings to the checked workstations.');setText('settings-impact-count',String(state.checkedScopes.size));setText('settings-scope-mode','Bulk selection');setText('settings-scope-warning',`Bulk update mode. Changes will apply to ${state.checkedScopes.size} workstations. Mixed values are highlighted.`);return;}if(state.activeScope&&String(state.activeScope).startsWith('ws-')){const workstation=workstationByScope(state.activeScope);setText('settings-selection-label',workstation?.text||'Selected workstation');setText('settings-selection-help','Review the workstation settings on the right, then save when you are ready.');setText('settings-impact-count','1');setText('settings-scope-mode','Single workstation');setText('settings-scope-warning','Changes apply to one workstation only.');return;}setText('settings-selection-label','No workstation selected');setText('settings-selection-help','Click one workstation to edit it, or check multiple workstations for a bulk update.');setText('settings-impact-count','0');setText('settings-scope-mode','No workstation selected');setText('settings-scope-warning','Choose one workstation or check several workstations to start editing.');};
const renderBulkSelection=()=>{const panel=bulkPanel();const list=bulkList();const empty=bulkEmpty();const count=bulkCount();if(!panel||!list||!empty||!count)return;const scopes=Array.from(state.checkedScopes);const hasSelection=scopes.length>0;panel.classList.toggle('hidden',!hasSelection);count.textContent=`${scopes.length} selected`;empty.classList.toggle('hidden',hasSelection);list.innerHTML='';if(!hasSelection){updateSelectionSummary();return;}scopes.forEach((scope)=>{const workstation=workstationByScope(scope);const pill=document.createElement('div');pill.className='inline-flex max-w-full items-center gap-2 rounded-full border border-sky-200 bg-white px-3 py-1.5 text-sm font-medium text-sky-700 shadow-sm';const label=document.createElement('span');label.className='truncate';label.textContent=workstation?.text||scope;const remove=document.createElement('button');remove.type='button';remove.className='inline-flex h-5 w-5 items-center justify-center rounded-full bg-white text-sky-600 transition hover:bg-sky-100';remove.innerHTML='&times;';remove.addEventListener('click',async()=>{state.checkedScopes.delete(scope);renderWorkstationList();renderBulkSelection();if(state.checkedScopes.size>0){const bulkScope=selectedBulkScope();if(bulkScope)await loadSettings(bulkScope);}else if(state.activeScope&&String(state.activeScope).startsWith('ws-')){await loadSettings(state.activeScope);}else{state.activeScope=null;resetForms();}});pill.appendChild(label);pill.appendChild(remove);list.appendChild(pill);});updateSelectionSummary();};
const renderFacilityFilter=()=>{const select=byId('settings-facility-filter');if(!select)return;select.innerHTML='<option value="">All facilities</option>';state.facilities.forEach((facility)=>{const option=document.createElement('option');option.value=facility.id;option.textContent=facility.text;if(facility.id===state.facilityFilter)option.selected=true;select.appendChild(option);});};
const renderWorkgroupFilter=()=>{const select=byId('settings-workgroup-filter');if(!select)return;const available=state.workgroups.filter((item)=>!state.facilityFilter||item.facilityId===state.facilityFilter);if(state.workgroupFilter&&!available.some((item)=>item.id===state.workgroupFilter))state.workgroupFilter='';select.innerHTML='<option value="">All workgroups</option>';available.forEach((workgroup)=>{const option=document.createElement('option');option.value=workgroup.id;option.textContent=workgroup.text;if(workgroup.id===state.workgroupFilter)option.selected=true;select.appendChild(option);});};
const filteredWorkstations=()=>{const query=normalizeSearch(state.search);return state.workstations.filter((workstation)=>{if(state.facilityFilter&&workstation.facilityId!==state.facilityFilter)return false;if(state.workgroupFilter&&workstation.workgroupId!==state.workgroupFilter)return false;if(!query)return true;return[workstation.text,workstation.workgroupText,workstation.facilityText].some((item)=>normalizeSearch(item).includes(query));});};
const workstationGroupKey=(workstation)=>`${workstation.facilityId}::${workstation.workgroupId}`;
const renderWorkstationList=()=>{const container=workstationList();if(!container)return;const visible=filteredWorkstations();setText('settings-workstation-count',`${visible.length} ${visible.length===1?'result':'results'}`);container.innerHTML='';if(visible.length===0){const empty=document.createElement('div');empty.className='rounded-xl bg-white px-4 py-5 text-sm text-slate-500';empty.textContent='No workstation matches the current search or filters.';container.appendChild(empty);return;}const groups=new Map();visible.forEach((workstation)=>{const key=workstationGroupKey(workstation);if(!groups.has(key)){groups.set(key,{facilityText:workstation.facilityText,workgroupText:workstation.workgroupText,items:[]});}groups.get(key).items.push(workstation);});const fragment=document.createDocumentFragment();Array.from(groups.values()).forEach((group)=>{const section=document.createElement('section');section.className='mb-3 last:mb-0';const header=document.createElement('div');header.className='mb-1.5 flex items-center justify-between px-2';header.innerHTML=`<div class="min-w-0"><p class="truncate text-sm font-semibold text-slate-800">${group.workgroupText}</p><p class="truncate text-xs text-slate-400">${group.facilityText}</p></div><span class="text-[11px] font-semibold uppercase tracking-[0.2em] text-slate-400">${group.items.length}</span>`;section.appendChild(header);const list=document.createElement('div');list.className='space-y-1.5';group.items.forEach((workstation)=>{const row=document.createElement('div');const isChecked=state.checkedScopes.has(workstation.scope);const isActive=state.checkedScopes.size===0&&state.activeScope===workstation.scope;row.className=`flex items-center gap-3 rounded-xl border px-3 py-2.5 transition ${isChecked?'border-sky-200 bg-sky-50':isActive?'border-sky-300 bg-white shadow-sm':'border-transparent bg-white hover:border-slate-200'}`;if(config.multiple){const checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.checked=isChecked;checkbox.className='h-4 w-4 rounded border-slate-300 text-sky-500';checkbox.addEventListener('change',async()=>{if(checkbox.checked){state.checkedScopes.add(workstation.scope);}else{state.checkedScopes.delete(workstation.scope);}renderWorkstationList();renderBulkSelection();if(state.checkedScopes.size>0){const bulkScope=selectedBulkScope();if(bulkScope)await loadSettings(bulkScope);}else if(state.activeScope&&String(state.activeScope).startsWith('ws-')){await loadSettings(state.activeScope);}else{resetForms();}});row.appendChild(checkbox);}const body=document.createElement('button');body.type='button';body.className='min-w-0 flex-1 text-left';body.innerHTML=`<p class="truncate text-sm font-semibold ${isActive?'text-sky-600':'text-slate-800'}">${workstation.text}</p><p class="truncate text-xs text-slate-400">${workstation.workgroupText} · ${workstation.facilityText}</p>`;body.addEventListener('click',async()=>{if(state.checkedScopes.size>0)state.checkedScopes.clear();state.activeScope=workstation.scope;renderBulkSelection();renderWorkstationList();await loadSettings(workstation.scope);});row.appendChild(body);if(isActive){const badge=document.createElement('span');badge.className='inline-flex rounded-full bg-sky-100 px-2.5 py-1 text-[11px] font-semibold text-sky-600';badge.textContent='Loaded';row.appendChild(badge);}list.appendChild(row);});section.appendChild(list);fragment.appendChild(section);});container.appendChild(fragment);};
const resetForms=()=>{state.currentData={};state.currentMeta={workstationCount:0,singleWorkstation:false,scopeType:null,mixedFields:[]};['units','LumUnits','AmbientStable','CalibrationPresents','CalibrationType','ColorTemperatureAdjustment','WhiteLevel_u_extcombo','UsedRegulation','UsedClassification'].forEach((fieldId)=>{const element=byId(fieldId);if(element)element._optionsSource=[];populateSelect(fieldId,[]);});fieldIds.forEach((fieldId)=>setInputValue(fieldId,''));fieldIds.forEach((fieldId)=>applyMixedState(fieldId,false));syncConditionalFields();updateSelectionSummary();};
const refreshClassificationOptions=async()=>{const regulation=byId('UsedRegulation')?.value;const scope=selectedBulkScope()||state.activeScope;if(!scope||!regulation)return;const response=await fetch(`/app-settings/get/categories?id=${encodeURIComponent(scope)}&regulation=${encodeURIComponent(regulation)}`);if(!response.ok)return;const payload=await response.json();const classification=byId('UsedClassification');if(classification)classification._optionsSource=payload;populateSelect('UsedClassification',payload,state.currentData.UsedClassification,state.currentMeta.mixedFields.includes('UsedClassification'));syncConditionalFields();};
const loadSettings=async(scope)=>{if(!scope)return;state.activeScope=scope;setText('settings-selection-help','Loading settings for the selected workstation scope.');const response=await fetch(`/app-settings/${scope}`);if(!response.ok){setText('settings-selection-help','Failed to load workstation settings.');return;}const payload=await response.json();state.currentData=payload.data||{};setScopeMeta(payload.meta||{});const options=payload.options||{};const selectSources={units:options.units,LumUnits:options.LumUnits,AmbientStable:options.AmbientStable,CalibrationPresents:options.CalibrationPresents,CalibrationType:options.CalibrationType,ColorTemperatureAdjustment:options.ColorTemperatureAdjustment_extcombo||options.ColorTemperatureAdjustment,WhiteLevel_u_extcombo:options.WhiteLevel_u_extcombo,UsedRegulation:options.UsedRegulation,UsedClassification:[]};Object.entries(selectSources).forEach(([fieldId,source])=>{const element=byId(fieldId);if(element)element._optionsSource=source;});populateSelect('units',options.units,state.currentData.units,state.currentMeta.mixedFields.includes('units'));populateSelect('LumUnits',options.LumUnits,state.currentData.LumUnits,state.currentMeta.mixedFields.includes('LumUnits'));populateSelect('AmbientStable',options.AmbientStable,state.currentData.AmbientStable,state.currentMeta.mixedFields.includes('AmbientStable'));populateSelect('CalibrationPresents',options.CalibrationPresents,state.currentData.CalibrationPresents,state.currentMeta.mixedFields.includes('CalibrationPresents'));populateSelect('CalibrationType',options.CalibrationType,state.currentData.CalibrationType,state.currentMeta.mixedFields.includes('CalibrationType'));populateSelect('ColorTemperatureAdjustment',options.ColorTemperatureAdjustment_extcombo||options.ColorTemperatureAdjustment,state.currentData.ColorTemperatureAdjustment,state.currentMeta.mixedFields.includes('ColorTemperatureAdjustment'));populateSelect('WhiteLevel_u_extcombo',options.WhiteLevel_u_extcombo,state.currentData.WhiteLevel_u_extcombo,state.currentMeta.mixedFields.includes('WhiteLevel_u_extcombo'));populateSelect('UsedRegulation',options.UsedRegulation,state.currentData.UsedRegulation,state.currentMeta.mixedFields.includes('UsedRegulation'));fieldIds.forEach((fieldId)=>setInputValue(fieldId,state.currentData[fieldId]));fieldIds.forEach((fieldId)=>applyMixedState(fieldId,state.currentMeta.mixedFields.includes(fieldId)));if(state.currentData.UsedRegulation){await refreshClassificationOptions();}else{const classification=byId('UsedClassification');if(classification)classification._optionsSource=[];populateSelect('UsedClassification',[], '', state.currentMeta.mixedFields.includes('UsedClassification'));}syncConditionalFields();renderWorkstationList();renderBulkSelection();updateSelectionSummary();};
const selectedScopes=()=>{const bulkScope=selectedBulkScope();if(bulkScope)return[bulkScope];return state.activeScope&&String(state.activeScope).startsWith('ws-')?[state.activeScope]:[];};
const saveSettings=async(action)=>{const scopes=selectedScopes();if(scopes.length===0){alert('Choose at least one workstation first.');return;}const formIdMap={app:'settings-form-application',dc:'settings-form-calibration',qa:'settings-form-qa',location:'settings-form-location'};const form=byId(formIdMap[action]);const formData=new FormData(form);['PutDisplaysToEnergySaveMode','CreateICCICMProfile','AutoDailyTests'].forEach((id)=>{if(byId(id)&&!byId(id).checked)formData.delete(id);});for(const scope of scopes){const response=await fetch(`/app-settings/save/${action}/${scope}`,{method:'POST',headers:{'X-CSRF-TOKEN':config.csrf},body:formData});if(!response.ok){alert('Failed to save settings.');return;}}setText('settings-selection-help','Changes were saved successfully.');};
const renderFilters=()=>{renderFacilityFilter();renderWorkgroupFilter();renderWorkstationList();renderBulkSelection();updateSelectionSummary();};
const loadHierarchy=async()=>{if(Array.isArray(config.initialTreeData)&&config.initialTreeData.length>0){buildHierarchyData(config.initialTreeData);renderFilters();return;}const response=await fetch(config.treeUrl,{headers:{'X-Requested-With':'XMLHttpRequest'}});if(!response.ok){setText('settings-selection-help','Failed to load the workstation browser.');return;}const payload=await response.json();buildHierarchyData(Array.isArray(payload)?payload:[]);renderFilters();};
const init=async()=>{await loadHierarchy();byId('settings-intro-close')?.addEventListener('click',()=>{introCard()?.remove();});byId('settings-workstation-search')?.addEventListener('input',(event)=>{state.search=event.target.value||'';renderWorkstationList();});byId('settings-facility-filter')?.addEventListener('change',(event)=>{state.facilityFilter=event.target.value||'';renderWorkgroupFilter();renderWorkstationList();});byId('settings-workgroup-filter')?.addEventListener('change',(event)=>{state.workgroupFilter=event.target.value||'';renderWorkstationList();});byId('settings-bulk-clear')?.addEventListener('click',async()=>{state.checkedScopes.clear();renderWorkstationList();renderBulkSelection();if(state.activeScope&&String(state.activeScope).startsWith('ws-')){await loadSettings(state.activeScope);}else if(config.initialScope&&String(config.initialScope).startsWith('ws-')){await loadSettings(config.initialScope);}else{state.activeScope=null;resetForms();}});if(config.initialScope&&String(config.initialScope).startsWith('ws-')){await loadSettings(config.initialScope);}else{resetForms();}byId('PutDisplaysToEnergySaveMode')?.addEventListener('change',syncConditionalFields);byId('ColorTemperatureAdjustment')?.addEventListener('change',syncConditionalFields);byId('WhiteLevel_u_extcombo')?.addEventListener('change',()=>{const selected=byId('WhiteLevel_u_extcombo').value;if(selected&&selected!=='native'&&selected!=='custom'){setInputValue('WhiteLevel',selected);setInputValue('SetWhiteLevel','true');}else{setInputValue('SetWhiteLevel',selected==='native'?'false':'true');}syncConditionalFields();});byId('UsedRegulation')?.addEventListener('change',refreshClassificationOptions);byId('UsedClassification')?.addEventListener('change',()=>setInputValue('UsedClassificationForLastScheduling',byId('UsedClassification').value));byId('ColorTemperatureAdjustment')?.addEventListener('change',()=>{if(byId('ColorTemperatureAdjustment').value!=='20'){setInputValue('ColorTemperatureAdjustment_ext','');}});document.querySelectorAll('.settings-save-btn').forEach((button)=>{button.addEventListener('click',()=>saveSettings(button.dataset.action));});syncConditionalFields();};
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init,{once:true});}else{init();}
})();
</script>
